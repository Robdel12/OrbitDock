.test-workstreams/
*.db
*.db-wal
*.db-shm

# Certificates (NEVER commit these)
*.p12
*.cer

# Build artifacts
*.zip

# Xcode
*.xcworkspace/
buildServer.json
# Note: Package.resolved is force-tracked (git add -f) for CI reproducibility
xcuserdata/
.claude/

# Swift Package Manager
.build/
.swiftpm/

# Node.js (Vizzly CLI)
node_modules/

# Vizzly
.vizzly/

# Test results
*.xcresult
*.trace/
.logs/
